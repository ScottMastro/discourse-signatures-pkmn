en:
  site_settings:
    signatures_enabled: "Enable user-made signatures below posts"
    signatures_visible_by_default: "Make signatures visible for logged in users by default"
    signatures_visible_for_guests: "Makes signatures visible for anonymous visitors"